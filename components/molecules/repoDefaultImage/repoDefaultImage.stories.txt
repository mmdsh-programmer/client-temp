import React from "react";
import { Meta, StoryObj } from "@storybook/react";
import RepoDefaultImage from ".";

export default {
  title: "Components/Molecules/RepoDefaultImage",
  component: RepoDefaultImage,
  args: {
    disabled: false,
  },
} as Meta<typeof RepoDefaultImage>;

export const Default: StoryObj<typeof RepoDefaultImage> = {
  render: (args) => <RepoDefaultImage {...args} onClick={(name) => alert(`Selected: ${name}`)} />,
};

export const Disabled: StoryObj<typeof RepoDefaultImage> = {
  args: {
    disabled: true,
  },
};
